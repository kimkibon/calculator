package com.nueral.calculator.repository.needExp;

import com.nueral.calculator.entity.needExp.NeedExp;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NeedExpRepository extends JpaRepository<NeedExp , Integer> {
}
